services:
  talenta-project:
    build: ./
    container_name: talenta-project
    restart: always
    env_file:
     - ./.env
    ports:
     - "127.0.0.1:3012:3000"
    networks:
     - project-network
networks:
  project-network:
    external: true